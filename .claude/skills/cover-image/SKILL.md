---
name: cover-image
description: Use when generating, replacing, or evaluating a blog article cover/eyecatch image — 記事のカバー画像・アイキャッチの生成・差し替え・候補評価を依頼されたとき、tools/imagegen（Gemini API）を使う作業全般
---

# カバー画像生成

`tools/imagegen/generate_image.py`（Gemini API）でカバー画像を生成し、評価 → 運営者選定 → 記事配置 → 検証まで行う。

## デザイン方針（2026-09-16 改定）

経緯：2026-08-18 に「tech カバーが一覧で全部同じ画像に見える」問題へ、固定＝濃紺の設計図調＋雲雀署名 / 可変＝主役モチーフ＋アクセント色 1 色、の暫定方針を入れた。4 本作って 400px で並べた結果、色を 1 色差し替えても濃紺の背景が画面の 7 割を占めて同じに見えることが分かった（区別できたのは面積の大きい部品に色を載せた 2 本のみ）。署名サイズの雲雀は 400px では見えず、ブランドの役割も果たしていなかった。2026-09-16 に運営者判断で固定部分を減らした。

同日、減らした後の「共通＝手描き風の線画」でも同じことが起きた。devcontainer 前後編の候補を淡い紙にインク線＋差し色 1 つで作ったところ、直前の ghostty-herdr-migration と同じ絵に見えた（紙の色を変えても型が同じなら区別できない。運営者指摘）。共通にするのは描き方ではなく世界観だけ、と再改定した。

- 共通（ブランドの役割）：写真でなくイラストであること、淡めで落ち着いた配色、tech に緑を使わないこと。この 3 点だけ。描き方は固定しない
- 可変（記事の識別）：描き方・背景色・構図・主役モチーフ・アクセント色。方針は記事ごとに、記事の趣旨・本文の内容・扱っている技術から考える。既存カバー（特に一覧で隣に来る直近の記事）を見て、同じテイストにしない。同じにした方がよい理由があるときだけ揃える（続き物など）。描き方の選択肢をこの文書に列挙しない——列挙すると次の回はそこから選び、結局同じ絵が増える（2026-09-16 に一度列挙して運営者指摘で削除）
- 続き物（前後編）は「同じにした方がよい理由がある」例。2 本を同じ描き方・同じ配色で作り、対だと分かるようにする。主役の配置は変える
- 雲雀は任意。入れるなら隅に小さく 1 羽、円形ストロークは付けない（主役サイズ＋円形ストロークはロゴ画像に見える）。入れる場合の参照画像は `reference-bird.png`
- 他社ロゴ：生成 AI に描かせない（形が崩れる）。使うなら実物ファイルを後から合成する。ライセンスは都度確認する（Apache-2.0 は §6 で商標の使用を許諾していない。出所表示の通常の範囲かどうかを見る）。ghostty-herdr-migration では合成まで試した上で運営者が「無くてよい」と判断した
- 色の決まり：
  - 緑は tech カバーに使わない。カテゴリチップの緑＝Life と同一カード上で意味が衝突する（design-direction.md §2、BlogCard.astro）。Life 記事カバーに温存
  - 彩度は春空の世界観に合わせ淡め。蛍光系は不可。カバーはコンテンツ扱いでサイトトークンの縛りは無いが、世界観からは外さない
  - 色名が違っても同系に見える組み合わせがある（濃紺の上の淡い藤色と桜色）。色は名前でなく 400px 比較で決める
- 判定基準：400px 縮小で既存カバーと並べ、一目で別画像と分かること（評価手順 2 で実施）
- 既存カバーの記録（2026-09-16 時点。避ける対象であって選択肢ではない）：濃紺の設計図調 2 本（building-this-blog / contact-form）、木目の机上イラスト 1 本（incorporating-bytelark）、灰白の紙にインク線画 1 本（ghostty-herdr-migration）、青灰の背景に輪郭線なしの平塗り 2 本（claude-code-devcontainer 前後編。対）

## 進め方（方針の確認 → 案の探索 → 深掘り）

1. 方針を立てる。記事の趣旨・本文・扱っている技術を読み、既存カバー（`src/content/posts/*/cover.*`。一覧で隣に来る直近の記事を特に）を 400px で並べて見る。そのうえで、この記事のカバーをどういうテイストにするかを決める
2. 生成する前に運営者に方針を確認する。書くのは 3 点：どういうテイストか／既存記事に同じテイストのものがあるか（あるなら揃える理由）／その方針が今回の記事に合う根拠。OK が出るまで生成しない
3. 方針の中で案を 3 つ書き、1 案 1 枚ずつ生成する（Flash 3 枚 ≒ 45 円）。3 案は構図か描き方の違いにし、色や細部の違いだけの 3 案にしない。方針を確認せず構図の 3 案から始めると、選んだ後に描き方でやり直しになる（devcontainer 前後編で実測、約 75 円の無駄）
4. 400px 比較を作って運営者に見せ、進める案を選んでもらう
5. 選ばれた案をそのまま採用するか、変えたい点を反映して数枚を追加生成するかは運営者判断
6. 生成後の合成・修正（白い部分の埋め、ロゴの合成、縮小）は Pillow で行い、手順を scratchpad のスクリプトに残す
7. 色の直しは再生成せず Pillow で試す。平坦な塗り面なら、最頻色を紙色とみなして差し替える／指定領域の色を置き換える／floodfill で塗る、で足りる（費用ゼロ。devcontainer 前後編で紙色 2 案・Mac の枠と画面・カメラの色を直した）

## 実行前の確認（毎回）

- 生成は有料（Flash 2K 1枚 $0.101 ≒ 15円 / Pro $0.134）。実行前に枚数と概算費用を提示して承認を得る
- モデルは Flash（`gemini-3.1-flash-image`、スクリプト既定）を主力にする。運営者の選好も Flash。Pro は注記がきれいだが線が細く、比較用の少数のみ
- 参照画像は雲雀を入れるときだけ `reference-bird.png` を渡す。入れないなら `-r` は不要

## 生成

```fish
cd tools/imagegen
.venv/bin/python generate_image.py -n 3 -p prompt_<記事slug>.txt \
  -r ../../docs/design-drafts/covers/reference-bird.png \
  -o out/<記事slug>
```

- 出力先は `-o out/<記事slug>` で必ず記事ごとのサブディレクトリを指定する（`out/` 直下にフラットに出さない。どの画像がどの記事か分からなくなるため）。slug は `src/content/posts/` のフォルダ名に合わせる
- プロンプトは記事ごとに `prompt_<記事slug>.txt` を新調する（デザイン方針節の共通・可変に従う。既存カバーと同じ描き方に揃えると決めたときだけ、その記事の `prompt_<slug>.txt` を雛形にする。新しい描き方なら雛形を使わず、構成（シーン／画風／色／注記／構図／制約の 6 節）だけ踏襲する）。「後でロゴを置くので隅を空けておく」のような指示は、その領域が白い矩形として描かれる（ghostty-herdr-migration で実測）。空けたい場所は指示せず、生成後に Pillow で埋める。`prompt_logo.txt` は雲雀主役の旧様式。新調するときの鉄則：1案 = 1リクエスト（複数案を1枚にまとめない）、注記の文字は英数字のみ許可（全面禁止も日本語も破綻する）
- 出力は `out/<記事slug>/` に raw（16:9 2K）と `*_cover.png`（40:21 センタークロップ、2752×1444）が並ぶ

## 評価と選定

1. 各候補の `*_cover.png` を原寸で確認：（雲雀を入れた場合）鳥の造形がロゴに忠実か／注記のラベル重複・文字化け・値のない寸法線／構図バランス
2. 400px 幅に縮小した画像も作って確認（ブログ一覧カード相当。細線・小要素はここで消える）。既存記事の cover と横並びにし、一目で別画像と分かるかを確認する（デザイン方針節の判定基準）
3. 候補を順位と根拠つきで運営者に提示し、選定を仰ぐ（勝手に決めない）

## 記事への配置

- 記事をフォルダ形式にし（`src/content/posts/<slug>/index.md`）、選定画像を同フォルダに `cover.png` で配置、frontmatter に `cover: ./cover.png`（詳細: docs/writing-workflow.md §画像）
- コミットする cover は 2000×1050 に縮小（Pillow LANCZOS）。2752 原本と縮小版は `out/<記事slug>/`（gitignored）に残す

## 検証と既知の罠

- CLAUDE.md §7 の検証ゲートを全て実施（ローカル + CF preview のデスクトップ/モバイル表示、CI green）
- カバー付き記事のパス変更（改名・フォルダ化）は CF ビルドキャッシュで ImageNotFound になる既知パターン。CF が赤になったら Clear Cache
- サブディレクトリ起点のセッションは repo 本体への Bash 書き込み（git 含む）が sandbox で塞がれる。その場合は bash スクリプトを書き出して運営者ターミナルで実行してもらう
