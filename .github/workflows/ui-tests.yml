name: UI Tests
on:
  push:
    branches: [main, "feat/*"]
  pull_request:
    branches: [main, "feat/*"]

# 同一 ref で新しい push が来たら進行中の古い run を打ち切る（runner 時間の無駄を防ぐ）
concurrency:
  group: ui-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    # 公式 Playwright コンテナ（ブラウザ + OS 依存を内蔵）。
    # これにより runtime の `playwright install --with-deps`（apt 実行）が不要になり、
    # Ubuntu 24.04(noble) 上で apt がハングしてジョブが timeout する問題を回避する。
    # タグは @playwright/test の解決バージョン(1.59.1)と一致させること。
    timeout-minutes: 20
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.59.1-noble
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Enable corepack
        run: corepack enable
      - name: Install dependencies
        run: yarn install --immutable
      - name: Build
        run: yarn build
      - name: Run Playwright tests
        run: yarn test:e2e
      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
